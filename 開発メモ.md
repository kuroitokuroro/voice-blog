
# 開発メモ

## 目標タスクの色名について
目標タスクの色分け状態をローカルストレージに保存する際、以下の名称を使っている。

* `green`
* `gray`

色スキン実装の際、同じ状態に対して別の名前を付けた。

* `--iro-goal-1`
* `--iro-goal-2`

数字を使用したのは、見た目の色がgreenやgrayになるとは限らないため。

保存の安全性を考慮し、localStorage 側の `green` / `gray` は変更しない。  
見た目の色だけを `--iro-goal-1` / `--iro-goal-2` で管理する。
