
# 開発メモ

## 目標タスクの色名について

目標タスクの色分け状態をローカルストレージに保存する際、以下の名称を使っている。

* `green`
* `gray`

色スキン実装の際、同じ状態に対して別の名前を付けた。

* `--iro-goal-1`
* `--iro-goal-2`

数字を使用したのは、見た目の色がgreenやgrayになるとは限らないため。

保存の安全性を考慮し、localStorage 側の `green` / `gray` は変更しない。  
見た目の色だけを `--iro-goal-1` / `--iro-goal-2` で管理する。

## PCとスマホで反応を変えたい場合

既存の以下の関数を参考にし、同じ思想で専用の関数を作る。

▼参考関数

isPcPointer()
→ 今の操作環境がPC寄りかどうかを判定する係

setupPcActionMenu()
→ PCではクリックで操作メニューを出す係

setupSwipeActions()
→ スマホではスワイプで操作を出す係

▼大きな流れ

操作環境を判定する
PCならクリック操作を使う
スマホならタッチ操作を使う
最後は共通の保存関数へ渡す

※既存の関数に機能を追加するのではなく、あくまで思想を使い回すこと。